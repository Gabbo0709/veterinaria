class Empleado {
  constructor (curp, nombre, nss, telefono, fechaNacimiento, categoria, rfc) {
    this.curp = curp
    this.nombre = nombre
    this.nss = nss
    this.telefono = telefono
    this.fechaNacimiento = fechaNacimiento
    this.categoria = categoria
    this.rfc = rfc
  }
}

export default Empleado
