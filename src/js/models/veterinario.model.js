class Veterinario {
  constructor (curp, nombre, telefono, direccion, rfc, nss, especialidad) {
    this.curp = curp
    this.nombre = nombre
    this.telefono = telefono
    this.direccion = direccion
    this.rfc = rfc
    this.nss = nss
    this.especialidad = especialidad
  }
}

export default Veterinario
